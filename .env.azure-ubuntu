# Optimized Environment Configuration for Azure Ubuntu Free Tier
# Copy this to backend/.env and frontend/.env as appropriate

# Database Configuration (SQLite - Optimized for Free Tier)
DATABASE_URL=sqlite:///./seatz.db

# Email Configuration (Required for notifications)
# Replace with your actual email settings
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=SeatZ Notifications <notifications@seatz.app>

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Environment Settings (Production Optimized)
ENVIRONMENT=production
DEBUG=False

# BRACU Connect API Configuration
BRACU_API_URL=https://bracu-connect-rg-hchcffasd6gnahdt.southeastasia-01.azurewebsites.net/raw-schedule
BRACU_API_TIMEOUT=30

# Background Task Intervals (Optimized for Free Tier)
SYNC_INTERVAL_MINUTES=30
NOTIFICATION_INTERVAL_MINUTES=60

# Security Settings (Production)
SECRET_KEY=your-secret-key-here-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Settings
CORS_ORIGINS=http://localhost:3000,http://your-vm-ip

# Rate Limiting (Conservative for Free Tier)
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_BURST=10